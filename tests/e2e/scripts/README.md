# E2E Test Scripts

| File | Purpose | Status |
|------|---------|--------|
| `generate-test.js` | Test scaffold generator (`npm run generate:test`) | Active |
| `dev-login-test.js` | Manual dev login verification | Utility — not part of CI |
| `prod-login-test.js` | Manual prod login verification | Utility — not part of CI |
| `manual-strategy-test.js` | Manual strategy flow test | Utility — not part of CI |
| `manual-strategy-test-v2.js` | Manual strategy flow test v2 | Utility — not part of CI |

**Note:** Only `generate-test.js` is used in the automated workflow. Other files are manual utility scripts for debugging.
