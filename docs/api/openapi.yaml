# OpenAPI Specification Placeholder
#
# This file should be generated from the running FastAPI server.
#
# To export the OpenAPI spec:
#   1. Start the backend: cd backend && python run.py
#   2. Export JSON: curl http://localhost:8000/openapi.json > openapi.json
#   3. Convert to YAML (optional): Use online converter or yq
#
# For now, access the interactive API docs at:
#   - Swagger UI: http://localhost:8000/docs
#   - ReDoc: http://localhost:8000/redoc
#
# The FastAPI-generated OpenAPI spec includes:
#   - All endpoint definitions
#   - Request/response schemas
#   - Authentication requirements
#   - Query parameters

openapi: 3.0.0
info:
  title: AlgoChanakya API
  version: 0.5.0
  description: Options Trading Platform API

servers:
  - url: http://localhost:8000
    description: Development server

paths:
  /api/health:
    get:
      summary: Health Check
      responses:
        '200':
          description: OK

# Full spec available at http://localhost:8000/openapi.json when server is running
